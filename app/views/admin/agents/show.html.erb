<div class="panel panel-default">
  <!-- Default panel contents -->
  <div class="panel-heading"><%= t(:agent_info) %></div>
  <div class="panel-body">
    <p><%= t(:name) %>: <%= @agent.name %></p>
  </div>

  <!-- Table -->
  <%= render :partial => "students"%>
</div>

